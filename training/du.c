#include <stdio.h>
/**
 * main - radius
 * Return: 0
 */

int main(void)

{
	int radius;


	
	printf("whats the radius?");
	scanf("%i", &radius);

	int cycle = radius * 2;
		
	printf("the whole cycle is: %i cm\n", cycle);
	return (0);
}
